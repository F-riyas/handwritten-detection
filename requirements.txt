# OCR and Image Processing
pytesseract==0.3.10
Pillow==9.5.0
opencv-python==4.8.0.76

# Optional but useful for working with arrays & preprocessing
numpy==1.24.3

# Jupyter Notebook (if you run experiments in notebooks)
notebook==7.0.0
